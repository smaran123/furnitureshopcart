  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.select(:category_id, Category.all.map {|c| [c.name, c.id]}, :prompt => "Select a Category") %>
  </div>

  <div class="form-group">
  <div>
    <%= f.label :name   %><br>
   </div>
   <div clas="col-sm-1"> 
    <%= f.text_field :name %>
  </div>
 

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, rows: 6 %>
  </div>
    <br />
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  </div>
  <% f.fields_for :images do |builder| %>
    <p>
      <%= builder.label :avatar, "Image File" %>
      <%= builder.file_field :avatar %>|<%= builder.link_to_remove "Remove", :class =>"btn btn-sm btn-danger" %>
      </p>
    <% end %>



<br/>
  <%= f.link_to_add "Add More", :images, :class =>"btn btn-primary" %>



<br/>
<div class="actions">
  <%= f.submit %>
</div>
