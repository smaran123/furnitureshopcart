<header>
  <nav class="navbar navbar-default navbar-fixed-top container-fluid" role="navigation">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <a class="navbar-brand" href="#">Home Furnishing</a>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

   <ul class="nav navbar-nav navbar-right">
   <% if !current_admin %>
<% Category.where("name is not null and category_show = #{true}").each do |cat| %>
      <li class="dropdown">

          <a class="dropdown-toggle" data-toggle="dropdown">
        <%= cat.name %>
        </a>


          <ul class="dropdown-menu">
<% Product.where("category_id = #{cat.id}").each do |product| %>
        <li><%= link_to product.name, admin_product_path(product)%></li>
    <% end %>
        </ul>     
     
     </li>
     <% end %>
     <% end %>
            <% if current_user%>

     
          <li>
             <%= link_to "Profile",  profiles_path %>
          </li>
          
            <li>
                <%=link_to("Log Out", destroy_user_session_path, :method => :delete)%>
            </li>
          <% end %>
          <li>
          <%= link_to("Sign up", new_user_registration_path)%>
          </li>
            <li>
            <%=link_to("Login", new_user_session_path)%>
            </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
</header>
<script>
  $("#")
</script>